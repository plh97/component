const {
    curry,
    reduce
} = require('lodash');


let compose = (x,y) => g => x(y(g))


// let compose = (f,g) => x => f(g(x))

let add = e => e+x
var add1 = 
var last = compose(head, reverse);

module.exports = last;